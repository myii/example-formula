# -*- coding: utf-8 -*-
# vim: ft=yaml
---
example:
  pkg:
    name: example
  rootgroup: root
  config: '/etc/example'
  service:
    name: example
  subcomponent:
    config: '/etc/example-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
